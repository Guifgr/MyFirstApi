<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="../public/css/style.css">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css">
    <script src="https://code.jquery.com/jquery-3.5.1.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/popper.js@1.16.1/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script><title>Guifg API</title>
</head>
<body>
  <div class="wrapper w-75 mx-auto" style="min-height: 100vh"> 
    <nav class="navbar navbar-expand-lg navbar-light center">
        <div class="container-fluid">
            <a href="/" class="navbar-brand mr-3">Inicio</a>
            <button type="button" class="navbar-toggler" data-toggle="collapse" data-target="#navbarCollapse">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarCollapse">
                <div class="navbar-nav">
                    <a class="navbar-brand" href="<%= json.html_url %>">GitHub</a>
                    <a class="navbar-brand" href="<%= json.html_url+"?tab=repositories" %>">Repositórios</a>
                    <a class="navbar-brand" href="<%= json.followers_url %>">Followers</a>
                    <% if(json.blog != ''){ %> 
                        <a href="<%= json.blog %>" class="navbar-brand">Site do programador!</a>
                    <% } %> 
                </div>
            </div>
        </div>    
    </nav>

    <div class="mx-auto" style="width: 35vh;">
        <img class="rounded-circle card-img-top w-100" src="<%= json.avatar_url %>" alt="Card image cap">
        <div class="card-body">
        <% if(json.name !== null){ %> 
            <h1 class="card-title"><%= json.name %> </h1>
        <% }else{ %> 
            <h1 class="card-title"><%= json.login %> </h1>
        <% } %>
        <p class="card-text"><%= json.bio %></p>
        <a href="<%= json.html_url %>" class="btn btn-primary">Meu Git</a>
        </div>
    </div>
  </div>
            
</body>
</html>